rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // This rule denies all access to Firebase Storage.
    // It's a temporary measure for debugging the simulation of security rules.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
